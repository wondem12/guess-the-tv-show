export interface TvShow {
  id: number;
  tvShowName: string;
  hint: string;
  poster: string;
}

export interface Statics {
  rightCount: number;
  wrongCount: number;
  hintCount: number;
}
